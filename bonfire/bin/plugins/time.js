// Generated by CoffeeScript 1.12.3
(function() {
  define(["mithril", "moment"], function(m, moment) {
    return {
      position: "after",
      name: "time",
      parent: "title",
      render: function(doc, renderBefore, renderInner, renderAfter) {
        var time, timeText;
        if (doc.time != null) {
          time = moment.utc(doc.time).local();
          timeText = time.format("M/D/h:mm");
          return [renderBefore(doc), m("p.time-stamp.black-text.right", [timeText, renderInner(doc)]), renderAfter(doc)];
        }
      }
    };
  });

}).call(this);
